/**
 * App.tsx — entry component.
 * Routing and providers are handled in routes/AppRouter.tsx.
 * This file is kept as a lightweight re-export to match the standard Vite convention.
 */
export { default } from './routes/AppRouter'
